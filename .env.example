# ============================================
# DEPLOYMENT MODE
# ============================================
# Options:
#   - traditional: Backend only (no Jazz, no offline)
#   - jazz_only: Jazz only (no backend needed!)
#   - hybrid: Both Jazz + Backend (recommended)
#   - offline_first: Jazz primary, backend fallback
# ============================================
APP_MODE=hybrid

# ============================================
# DATABASE (only needed if APP_MODE != jazz_only)
# ============================================
# Database Configuration
DATABASE_URL=sqlite:///./reflx.db
# For PostgreSQL: postgresql://user:password@localhost:5432/dbname

# Admin Configuration
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=your-secure-password-here  # REQUIRED: Set a strong password for production

# Application Configuration
APP_ENV=development
LOG_LEVEL=INFO

# Cache & Offline Configuration
ENABLE_OFFLINE_MODE=true
MAX_QUEUE_SIZE=50
SYNC_RETRY_ATTEMPTS=3
SYNC_RETRY_DELAY=2

# Security (Production)
SECRET_KEY=your-secret-key-here

# ============================================
# JAZZ SYNC (needed if APP_MODE = jazz_only, hybrid, or offline_first)
# ============================================
JAZZ_SYNC_SERVER=wss://cloud.jazz.tools
# For self-hosted: wss://your-sync-server.com
# For local development: ws://localhost:4000
# For no network sync (local-only): leave empty

JAZZ_ENABLE_P2P=true
JAZZ_AUTH_PROVIDER=anonymous
# Options: anonymous, clerk, custom

JAZZ_STORAGE_ENCRYPTION=true

# ============================================
# QUICK START EXAMPLES
# ============================================
# No backend needed: APP_MODE=jazz_only JAZZ_SYNC_SERVER=wss://cloud.jazz.tools
# Local only: APP_MODE=jazz_only JAZZ_SYNC_SERVER=
# Full featured: APP_MODE=hybrid
